# 用户指南 QA Pipeline（对齐故障管理流程）

## 输入准备

从 DeepSeek-OCR `text_chunks_*.json` 转换：

```bash
/home/wugk/.conda/envs/dataflow/bin/python /home/wugk/finetune/finetune/test/ocr_chunks_to_dataflow.py \
  -i /home/wugk/finetune/finetune/data/用户指南/text_chunks_20251119_232705.json \
  -o /home/wugk/finetune/finetune/data/用户指南/text_chunks_20251119_232705_converted.json \
  --min-chars 80 --max-chars 4000
```

结果：276 原始 → **221 chunks**（过滤短碎片/纯表格 + 去重 1 条）

## 运行配置（与故障管理相同）

| 项 | 值 |
|---|---|
| 输入 | `text_chunks_20251119_232705_converted.json` |
| 缓存前缀 | `user_guide_converted_qa` |
| API | `http://127.0.0.1:19000/v1/chat/completions` |
| 模型 | `Qwen3.6-27B-Q5_K_M.gguf` |
| 并发 | `DF_MAX_WORKERS=1` |
| 每 chunk 题数 | 4 |
| distill_tag | 运维 |
| 断点续跑 | `QA_RESUME=1` |

## 脚本

- 前台：`/home/wugk/.cursor/plans/脚本04_前台运行_用户指南数据集.sh`
- 后台：`/home/wugk/.cursor/plans/脚本06_后台运行_用户指南_本地llama-server.sh`
- 进度：`QA_OUTPUT_DIR=.../用户指南 QA_FILE_PREFIX=user_guide_converted_qa 脚本02_查看QA管道运行进度.sh`

## 导出（Pipeline 完成后自动执行）

`脚本07_QA管道主程序_Python入口.py` 在 step21 结束后自动：

1. `convert_step15_to_qa.py 21 --with-context --no-audit`
2. 复制到 `{prefix}_sft.jsonl` / `{prefix}_sft.json`

即：`user_guide_converted_qa_sft.jsonl`（instruction + input=raw_content + output）

## 预估

- 221 chunks × 4 题 ≈ 884 初始 QA
- 经 21 步过滤后预估 **500–800 条**（参考故障管理 584→1544 的留存率）
